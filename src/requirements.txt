pandas>=2.0
numpy>=1.26,<2     # <2 keeps the numpy-1.x-compiled wheels (scipy/matplotlib/numba) ABI-safe; TF needs >=1.26
requests>=2.28
geopandas>=0.14
shapely>=2.0
pyproj>=3.5
scipy>=1.10
osmnx>=1.9
eurostat>=0.2
pypsa>=0.26
pyarrow>=14.0
fastapi>=0.111
uvicorn[standard]>=0.29

# --- Post-Pareto CNN site analysis (src/model/site_analysis) ---
tensorflow-cpu>=2.13      # runs the pretrained Google Dynamic World SavedModel
pystac-client>=0.7        # query the Planetary Computer STAC catalog
planetary-computer>=1.0   # sign Planetary Computer asset hrefs
odc-stac>=0.3             # load STAC items into xarray at a fixed grid/resolution
rasterio>=1.3             # raster IO + affine transforms + reprojection
rioxarray>=0.15           # xarray <-> rasterio bridge (CRS, transform, reproject)
Pillow>=10.0              # render the site PNG (true-colour S2 + patch overlay)
